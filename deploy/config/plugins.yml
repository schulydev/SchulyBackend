# Plugins the backend loads on startup (downloaded from the registry, hot-loaded).
# Manage at runtime via POST/DELETE /api/plugins/* — those rewrite this file.
plugins:
  - name: Schuly.Plugin.Schulware
    version: latest
